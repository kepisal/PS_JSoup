package Model;

import java.io.File;
import java.io.FileWriter;
import java.util.Arrays;
import java.util.concurrent.SynchronousQueue;
import java.util.regex.Matcher;
import java.util.regex.Pattern;

import org.jsoup.Jsoup;
import org.jsoup.nodes.Document;
import org.jsoup.nodes.Element;
import org.jsoup.select.Elements;

import Browser.mybrowser;
import utilities.Logger;

public class kaymu extends mybrowser {
	static Document kaymu_document;
	private static kaymu kaymu_obj = null;
	private kaymu(){ } 
	
	public static kaymu getLogger() {// singleton pattern
		if (kaymu_obj == null)
			kaymu_obj = new kaymu();
		return kaymu_obj;
	}
		
	public String getScript(){
		String result=null;
		
		return result; 
	}
	
	
	public static void main(String[] args) {
		try {
			
			
		} catch (Exception e) {
			// TODO: handle exception
			Logger.writeLogException(e, "A", "B");
		}
	}
}
