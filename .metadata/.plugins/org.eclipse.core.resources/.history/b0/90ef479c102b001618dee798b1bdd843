package Model;

import java.util.concurrent.SynchronousQueue;
import java.util.regex.Matcher;
import java.util.regex.Pattern;

import org.jsoup.Jsoup;
import org.jsoup.nodes.Document;
import org.jsoup.nodes.Element;
import org.jsoup.select.Elements;

import Browser.mybrowser;
import utilities.Logger;

public class kaymu extends mybrowser {
	public static void main(String[] args) {
		try {
			/*Document doc = Jsoup.connect("http://www.memrise.com/course/115/korean-alphabet/1/garden/learn/")
					.timeout(5000)
					.get();
			System.out.println(doc.);*/
			String a="http://www.memrise.com/course/115/korean-alphabet/1/garden/learn/";
			String pattern="com\\d+\\1";
			Pattern p=Pattern.compile(pattern);
			Matcher m=p.matcher(a);
			if(m.find()){
				System.out.println(m.group(0));
			}
			/*String from="com/";
			System.out.println(a.substring(a.indexOf(from)+from.length()-1,a.length())); // from to end
			String bgin="com/";
			String end="garden";
			System.out.println(a.indexOf(end));
			System.out.println(a.substring(a.indexOf(bgin),a.indexOf(end)));*/
			//System.out.println(s);
		} catch (Exception e) {
			// TODO: handle exception
			//Logger.writeLogException(e, "A", "B");
		}
	}
}
